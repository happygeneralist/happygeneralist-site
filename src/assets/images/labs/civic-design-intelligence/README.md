# Civic Design Intelligence images

Put source images for the Civic Design Intelligence Lab page in this folder.

Required current file:

```text
civic-design-intelligence-snapshot.webp
```

The Lab entry already points to:

```yaml
preview_image:
  src: labs/civic-design-intelligence/civic-design-intelligence-snapshot.webp
```

Because this folder is under `src/assets/images/`, Astro can optimise the image through `ResponsiveImage.astro`.
